# 路由器测试框架 - 安装使用指南

## 📦 快速开始

### 一键安装（推荐）

在任何 Windows 电脑上：

1. **拷贝安装脚本**
   ```
   将 "完整安装_含GitHub.bat" 拷贝到目标电脑
   ```

2. **运行安装**
   ```
   右键 → 以管理员身份运行
   ```

3. **按提示输入 Git 信息**
   ```
   用户名：heyp
   邮箱：heyp@milesight.com
   ```

4. **等待完成**（约 10 分钟）
   - 自动安装所有软件和依赖
   - 自动克隆项目代码
   - 自动启动测试框架

5. **开始使用**
   ```
   浏览器访问: http://localhost:5000
   ```

---

## 🛠️ 可用脚本说明

### 1. 完整安装_含GitHub.bat ⭐ 主安装脚本

**功能：** 完整的一键安装解决方案

**包含：**
- ✅ 安装 Chocolatey 包管理器
- ✅ 安装 Python 3.12
- ✅ 安装 Git
- ✅ 安装 GitHub Desktop（桌面快捷方式）
- ✅ 配置 Git 用户信息（交互式）
- ✅ 配置 Git 全局设置（编码、换行等）
- ✅ 克隆项目代码到 E:\GIT\ROUTER_TEST
- ✅ 安装所有 Python 依赖（含 paramiko、PyYAML 等）
- ✅ 自动处理依赖编译问题
- ✅ 启动测试框架

**使用场景：**
- 新电脑首次安装
- 完整环境搭建
- 需要 GitHub Desktop 的场景

**前置条件：**
- 管理员权限
- 网络连接

---

### 2. 修复依赖安装.bat 🔧 快速修复工具

**功能：** 修复依赖安装问题

**包含：**
- 重新安装所有 Python 依赖
- 使用预编译包避免编译错误
- 验证所有模块是否正确导入

**使用场景：**
- 依赖安装失败
- 缺少某些模块（如 paramiko）
- PyYAML 编译错误
- 更新依赖版本

**前置条件：**
- 已安装 Python
- 项目代码已克隆到 E:\GIT\ROUTER_TEST

**运行方法：**
```
cd E:\GIT\ROUTER_TEST
右键 → 以管理员身份运行 "修复依赖安装.bat"
```

---

## 📍 安装位置

安装完成后的文件位置：

### 软件安装位置
```
Python:          C:\Python312\
Git:             C:\Program Files\Git\
GitHub Desktop:  C:\Users\<用户名>\AppData\Local\GitHubDesktop\
Chocolatey:      C:\ProgramData\chocolatey\
```

### 项目代码位置
```
项目根目录:      E:\GIT\ROUTER_TEST\
当前分支:        RouterAuto
```

### 桌面快捷方式
```
GitHub Desktop   - 自动创建
```

---

## 📦 安装的软件和依赖

### 系统软件
- **Chocolatey** - Windows 包管理器
- **Python 3.12** - Python 运行环境
- **Git** - 版本控制工具
- **GitHub Desktop** - Git 图形界面工具

### Python 依赖包
```
Flask==2.3.3              # Web 框架
requests>=2.25.0          # HTTP 请求
PyYAML>=5.4.1            # YAML 配置解析
selenium==4.12.0          # 浏览器自动化
webdriver-manager==4.0.1  # 浏览器驱动管理
pyserial==3.5            # 串口通信
paho-mqtt>=2.0.0         # MQTT 客户端
openpyxl>=3.0.0          # Excel 文件处理
paramiko>=2.7.0          # SSH 客户端
```

---

## ⚙️ Git 自动配置

安装脚本会自动配置以下 Git 设置：

### 用户信息
```bash
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"
```

### 编码设置（避免中文乱码）
```bash
git config --global core.quotepath false
git config --global gui.encoding utf-8
git config --global i18n.commit.encoding utf-8
git config --global i18n.logoutputencoding utf-8
```

### 其他设置
```bash
git config --global core.autocrlf false        # 换行符处理
git config --global init.defaultBranch main    # 默认分支名
git config --global credential.helper wincred  # 凭证缓存
```

---

## 🚀 启动项目

### 方式 1：自动启动
安装完成后会自动启动，或运行：
```
E:\GIT\ROUTER_TEST\start_admin.bat
```

### 方式 2：手动启动
```bash
cd E:\GIT\ROUTER_TEST
python app.py
```

### 方式 3：从任意位置启动
```bash
cd /d E:\GIT\ROUTER_TEST && python app.py
```

启动后访问：**http://localhost:5000**

---

## 🎯 GitHub Desktop 使用

安装完成后：

### 1. 打开 GitHub Desktop
- 双击桌面快捷方式
- 或从开始菜单搜索 "GitHub Desktop"

### 2. 登录 GitHub 账号
- 点击 "Sign in to GitHub.com"
- 输入 GitHub 用户名和密码
- 完成授权

### 3. 添加本地仓库
- 点击 "File" → "Add Local Repository"
- 选择 `E:\GIT\ROUTER_TEST`
- 点击 "Add Repository"

### 4. 同步代码
- **Fetch origin** - 检查远程更新
- **Pull origin** - 拉取最新代码
- **Push origin** - 推送本地修改

---

## ❓ 常见问题

### Q1: 安装失败怎么办？

**A:** 检查以下几点：
1. 是否以管理员权限运行
2. 网络连接是否正常
3. 能否访问 GitHub 和 Chocolatey 网站
4. 杀毒软件是否拦截

**解决方法：**
- 重新以管理员身份运行脚本
- 检查防火墙设置
- 临时关闭杀毒软件

---

### Q2: PyYAML 安装失败？

**错误信息：**
```
ERROR: Failed to build 'PyYAML' when getting requirements to build wheel
```

**原因：** PyYAML 需要 C++ 编译器

**解决方法：**
- 脚本已自动使用预编译包（`--prefer-binary`）
- 如果仍然失败，运行 `修复依赖安装.bat`
- 或手动安装：`pip install PyYAML --only-binary :all:`

---

### Q3: paramiko 模块缺失？

**错误信息：**
```
No module named 'paramiko'
```

**解决方法：**
```bash
pip install paramiko --prefer-binary
```

或运行 `修复依赖安装.bat`

---

### Q4: GitHub Desktop 没有安装？

**检查方法：**
1. 查看桌面是否有快捷方式
2. 检查 `%LOCALAPPDATA%\GitHubDesktop\` 目录

**解决方法：**
- 重新运行安装脚本
- 或手动下载：https://desktop.github.com/

---

### Q5: Git 用户信息配置错误？

**查看当前配置：**
```bash
git config --global user.name
git config --global user.email
```

**重新配置：**
```bash
git config --global user.name "新用户名"
git config --global user.email "新邮箱"
```

---

### Q6: 安装速度很慢？

**原因：** 网络问题

**解决方法：**
- 脚本已使用清华镜像加速 pip 安装
- Chocolatey 和 Git 下载可能较慢，请耐心等待
- 可以手动下载软件后安装，然后运行脚本会跳过已安装的软件

---

### Q7: 如何在多台电脑上安装？

**方法：**
1. 将 `完整安装_含GitHub.bat` 拷贝到目标电脑
2. 以管理员身份运行
3. 按提示输入 Git 信息
4. 等待自动完成

**注意：** 每台电脑都需要单独运行一次安装脚本

---

### Q8: 如何更新代码？

**方式 1：使用 GitHub Desktop**
- 打开 GitHub Desktop
- 点击 "Fetch origin"
- 点击 "Pull origin"

**方式 2：使用 Git 命令**
```bash
cd E:\GIT\ROUTER_TEST
git pull origin RouterAuto
```

---

### Q9: 如何卸载？

**卸载步骤：**

1. **删除项目文件**
   ```
   删除 E:\GIT\ROUTER_TEST 文件夹
   ```

2. **卸载软件（可选）**
   ```bash
   choco uninstall python git github-desktop
   ```

3. **删除配置（可选）**
   ```
   删除 C:\Users\<用户名>\.gitconfig
   ```

---

## 🔧 高级用法

### 手动安装单个依赖

```bash
pip install <包名> --prefer-binary -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 更新所有依赖

```bash
cd E:\GIT\ROUTER_TEST
pip install -r requirements.txt --upgrade
```

### 查看已安装的包

```bash
pip list
```

### 验证依赖完整性

```bash
python -c "import flask, yaml, selenium, serial, paho.mqtt.client, paramiko; print('所有依赖正常')"
```

---

## 📚 相关文档

项目中的其他重要文档：

- **CLAUDE.md** - 项目概述和开发指南
- **PROJECT_STRUCTURE.md** - 项目结构规范（必读）
- **DMVPN_SERVER_GUIDE.md** - DMVPN 功能文档
- **requirements.txt** - Python 依赖列表

---

## ⚠️ 重要提示

1. **管理员权限**
   - 所有安装脚本都需要管理员权限
   - 修改网络配置需要管理员权限

2. **网络要求**
   - 需要能访问互联网
   - 需要能访问 GitHub
   - 会使用清华镜像加速下载

3. **Git 用户信息**
   - 用于所有 Git 提交记录
   - 建议使用真实姓名和公司邮箱
   - 安装时会交互式提示输入

4. **路径固定**
   - 项目默认安装到 `E:\GIT\ROUTER_TEST`
   - 如需修改，请编辑安装脚本中的路径

5. **分支固定**
   - 自动克隆 `RouterAuto` 分支
   - 不会影响其他分支

---

## 📞 技术支持

如果遇到问题：

1. 查看安装脚本输出的错误信息
2. 检查本文档的"常见问题"部分
3. 运行 `修复依赖安装.bat` 尝试自动修复
4. 检查网络连接和防火墙设置

---

**文档版本：** 2.0
**更新时间：** 2025-11-26
**适用系统：** Windows 10/11
**Python 版本：** 3.12+
